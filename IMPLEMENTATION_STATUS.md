# Investor Intelligence v2.0.0 — Final Release Truth

_Last reconciled: 2026-08-28 Asia/Taipei_

## Authoritative state

GitHub Support completed the platform-side sensitive-history purge for the affected pull-request internal references and cleared the unreferenced historical commits. The repository remains private; the application is released by verified private direct delivery and contains no inherited Git history.

The exact source commit becomes the accepted v2.0.0 source only when both exact-head workflows succeed on the same `main` commit:

1. `Final History and Support Purge Confirmation` — independently checks that the sensitive historical commit and affected pull refs are inaccessible, fetches every remaining GitHub-managed pull ref into a fresh isolated Git object database, and requires `--scope all --require-clean` with finding count `0`.
2. `Formal Private Release Package` — runs the complete retained Python, Worker, Phase 1–8, supply-chain, package-verification, reproducibility and clean-install gates and produces the exact acceptance receipt and release evidence.

An older successful run never transfers to a newer commit.

## Final application boundary

The final application package includes a double-click `install-final.cmd`, SHA-256-verifying `install-final.ps1`, package-contained `bootstrap.ps1`, local launcher, explicit opt-in scheduler and data-preserving uninstaller. Installation uses verified portable CPython 3.12.10 and the committed hash-locked dependency set. An intentional force reinstall preserves generated data, reports and the local briefing log.

Installation and release workflows do **not** deploy or enable LINE, Cloudflare, KV writes, external-user admission, memory, IBKR, brokerage access, billing or paid fallbacks. Scheduled tasks remain disabled unless the user explicitly runs `register-task.ps1 -Enable`.

## Privacy and architecture invariants

- owner/local configuration remains outside tracked source and uses ignored local files;
- shared LINE remains public-research-only and cannot access IBKR, brokerage accounts, holdings, quantities, cost/P&L, owner watchlists, local reports or private synchronization;
- direct-chat HMAC allowlisting is the only supported admission model and remains disabled until a separate deployment transaction;
- public, tenant-private and ephemeral-security state require physically separate KV bindings;
- no source is activated automatically; runtime work remains request/byte/time/retry/concurrency bounded;
- shared live option BID/ASK remains unavailable until a separately reviewed zero-cost source satisfies lawful automated-access and redistribution requirements;
- the deterministic manual calculator accepts only user-supplied figures and labels them unverified;
- no automatic trading or brokerage-write path exists.

## Release-state interpretation

Committed development-state JSON remains fail-closed by design and is excluded from the application ZIP. Final package readiness is established by the exact-head ephemeral release status and final acceptance receipt generated by the successful formal package workflow.

```text
APPLICATION_VERSION = 2.0.0
DISTRIBUTION_SCOPE = private_direct_delivery
GITHUB_SUPPORT_PURGE = completed
GIT_HISTORY_INCLUDED_IN_PACKAGE = false
DEPLOYED = false
LINE_PRODUCTION_ENABLED = false
IBKR_CONNECTED = false
EXTERNAL_USERS_ADMITTED = false
PAID_FALLBACK = forbidden
SCHEDULES_DEFAULT = disabled
FINAL_SOURCE_TRANSACTION = non_force_fast_forward_of_all_official_branches
FINAL_ARTIFACT = requires_both_exact_head_workflows_PASS
```

## Local action required

None for repository acceptance. No credential, token, raw LINE identifier, private message, portfolio or brokerage-account detail is required to build or verify the final release.
