# Current state / 目前狀態

Updated: 2026-09-12. Release identity: [README](../README.md). Previous maintenance state: `git show 1ba1a97713f4b152ffcb3cae4950c17076c8d0d0:state/STATUS.md`; earlier full history: `git show 4ba2a606c9e459884e15354aa91798ec432df0ba:state/STATUS.md`; older lookup: `git show 38860e7:state/STATUS.md`. History is not current acceptance or authorization.

## Scope and identity / 本輪範圍

使用者要求補齊既有圖文選單 **TOP20／宏觀產業分析／期權**，註記公司原文／中文名稱，提供量化訂單、履約時程及6個月／1年／2年實現與未實現情境；依提供的黑白貓咪／綠色金融圖片改善UI。優先次序：**LINE上線 → UI/UX → EXE模型切換確認與GitHub更新**。

Fetched starting HEAD **`1ba1a97713f4b152ffcb3cae4950c17076c8d0d0`**, branch `fix/options-provenance-audit`, upstream `8178f72e1acb5d36cf51212a815a1097a28fdf3a` (ahead46 at start). One writer in `_workspace/source`; evidence `_workspace/audit-runtime/followup-20260912`. Original untracked `HOTFIX-REFS.json`, `InvestorIntelligence.exe`, `cloud/wrangler.v213.production.local.toml` remain untouched and excluded from commits. Mixed installed parent is not a fixture.

Local commits: `ea2e09c66ac34b5acca007591a2f3a89fc1e8c01` (storage audit/journal test repair), `9a5a71cf58cf2f0fe1b1f88dba82431496db7127` (explicit R15 order reconciliation). Menu/theme/docs changes are the commit containing this STATUS; resolve with `git log -1 -- state/STATUS.md`. These are not an installed/released identity.

**No actual runtime installation, Production Worker/KV/storage/schedule change, real LINE send, credential/billing operation, real-model switch or preset change. No push or workflow dispatch in this slice.** Local tests include isolated installer/compiled-EXE fixtures and synthetic model transports, not installation into the live runtime. LINE/GitHub requests are recorded, but release qualification and concrete external-operation scope still require resolution; old activation approvals are not replayed.

Earlier separately requested `pi update --extensions` completed (SoL-Pi `2138669→d7ecfc0`); later unmet dependencies were observed but their origin remains unestablished. No additional dependency/package installation here.

## Corrections / 修正與接線

- **Storage test/gate debt closed locally:** the negative test replaced a retired source string and did not inject its fault. It now asserts the seam exists and tests both public adapters against private/security reads, writes, missing exports and missing pinned reader. Reviewed storage migration at `91fd323` already removed the old pointer reader; updated only its stale exact audit pin. Old-reader/arbitrary-drift negatives and actual Worker public-only reads pass. `storage.ts` and certified `qa.ts` unchanged.
- **Journal8 errors closed locally:** retained synthetic archive at265 path characters exists and matches the original bytes/SHA, while ordinary Python IO raises `FileNotFoundError`. Tests now read the deterministic owned archive through the extended Windows path after path checks, validate the recorded path and compare exact original bytes. No system long-path setting or production recovery code changed. Original failure logs and archives retained.
- **New-member order bug:** reconciliation formerly depended on the mutable historical `h6b.generic_sec_outlook` symbol; a fresh process could use the retired first-amount matcher. It now explicitly calls the existing R15 semantic chain. The actual CLI with synthetic SEC transport rejects unrelated revenue and year-only outlooks, retains explicit RPO/recognition quantities and does not use a model. “很多／幾乎全部” alone is not a numeric estimate. Same membership avoids a needless SEC map fetch. Existing baseline order text/dates are not newly researched; inherited report retrieval stamps are not order-refresh proof.
- **Three menu routes wired:** exact manager-configured commands now use the actual authorized/rate-limited caller and one pinned public view, not compact Q&A. TOP20 retains20 seven-field cards and bound company evidence/text actions. Macro returns the current20-company industry counts/percentages plus explicit `MACRO_PRODUCT_NOT_SEALED`; counts are not market weights or allocation advice. Options routes to existing quote queries and manual-calculation help while retaining `OPTION_DATA_UNAVAILABLE`.
- **UI source:** shared monochrome/dark-green tokens, labelled original company name, visible unverified Chinese-name state, clear historical returns vs realized/delayed/not-realized6/12/24-month scenario gap. No guessed Chinese dictionary, restored `TOP20_SENSITIVITY` price ranges or raw candidate macro values. Navigation and unavailable messages are not completion of the missing research products.

Details: [LINE UI](../docs/LINE_TOP20_UI.md), [research execution audit](../docs/RESEARCH_EXECUTION_AUDIT.md), [preserved report contract](../docs/DETAILED_REPORT_CONTRACT.md).

## Verification / Commands and results

- Targeted unchanged-source baselines reproduced both storage failures and the journal errors: `baseline-kv.log`, `baseline-owner-gate.log`, `baseline-journal.log`. `archive-path-proof.json` records ordinary-path failure versus matching extended-path bytes. No failure/receipt deletion.
- `python312/python.exe -m unittest discover -s tests -p 'test_*.py' -v`: **957 tests, failures=0, errors=0, skipped=2**, **277.440s**, exit0. Previous maintenance run was952/2 failures/8 errors/2 skips. Both retained skips are unavailable symlink creation, not waived storage/journal cases. Log `full-python.log`.
- Worker `npm run typecheck` PASS; full `npm test -- --reporter=default --reporter=json ...`: **521/521 PASS**. `full-typecheck.log`, `full-worker.json`, `full-worker.log`. Actual caller tests cover all3 menu commands, macro counts, invalid/stale/future refusals, quote/help navigation and explicit deep-product non-substitution; LINE is mocked.
- Initial menu typecheck caught a missing synthetic test env `LINE_CHANNEL_SECRET`; corrected and rerun. Failed `menu-typecheck.log` retained. A STATUS rewrite omitted the required `38860e7` history link; restored after the entrypoint test caught it. These intermediate failures remain logged.
- Documentation boundary/structure, workflow supply chain, Actions storage, security, canonical candidate, KV isolation and owner delivery gates: **8/8 PASS**. `compileall`, `pip check`, `git diff --check` PASS. No lock/dependency change or new workflow.
- Full Python includes **84/84 tracked PS1 parsing on PS5.1/7**,12 actual pure guard good/bad/missing cases per host and max-expression checks. Separate isolated `test_v213_activation_core.ps1` and `sync-v213-activation-bundle.ps1 -SelfTest` PASS on both hosts. No installed audit/normalization entrypoint or operation mutex used as a fixture.
- Full Python also compiles and executes the actual launcher with profile persistence, thinking UI, selection, child propagation and routing checks. Synthetic catalog/model transport tests reject redirects, malformed/oversized data, wrong model, incomplete output and profile drift. **This is not live installed EXE→gateway→Worker acceptance or switching the real Router model.**
- `source-comparison.json`:11 protected files byte-identical to starting HEAD, including `qa.ts`, `storage.ts`, production journal recovery, Worker/Python locks, scoring, compact policy, publication contract and all canonical SKILL/reference files. SKILL full execution/model adherence are still unverified; deterministic navigation is not a research run.
- `menu-replies.json` contains actual synthetic caller payloads; `menu-preview.html` and resized supplied artwork are outside source/runtime payloads. **HTML generated, visual review not completed:** managed browser rejected the local file URL and navigation from blank timed out. `browser-preview-status.json` retains this limitation; no extra preview server started. No device screenshot or delivery proof claimed.
- Remote R75 latest inspected PASS remains run `34422382127` on `ec03f044…`, not this source; listed PR audits skipped. No matching-HEAD remote qualification, source-bound live proof, immutable package or release receipt.

## Open findings / 未完成（不刪需求）

1. **Research delivery gap remains:** no admitted bilingual company identity/product path, per-order ledger, full20-company SKILL run or separate sealed data/narrative valuation products. Need original passages/URLs/hashes, security/share-class identity, currency/units, counterparty, cancellation/recognition periods, independent lineage and source rights. RPO aggregate is not individual contracts; overlapping RPO/backlog/prepayment cannot be summed. Some business models do not disclose meaningful orders; do not manufacture them to fill every row.
2. **6/12/24-month numbers remain unavailable:** require sourced shipment/recognition→revenue/margin/tax/capex/working capital→funding/dilution→appropriate valuation, dated reference price and explicit assumptions. Show realized, delayed/partial and not-realized outcomes separately. No unsupported probabilities; missing operands are not0% growth or0% loss. Current card text does not close this requirement.
3. **Prior P2 options:** current sealed payload lacks usable `options:latest`; a working navigation/help path does not supply Bid/Ask/Delta or qualified order parameters. Need versioned admission and pinned consumer proof, not direct-key carryover. Macro full-product admission is also absent; source HEALTHY or `publication_eligible=false` candidates cannot be promoted into reports.
4. **Prior P2 installer identity:** installed HOTFIX-REFS labels stale source `2b9e143`; file integrity differs from source-bound release identity. Need fresh packaging/receipt/install proof; do not silently rewrite refs. Actual model switch/completed-answer proof remains open.
5. **Prior P2 automation:** natural07:20/20:20 Taipei slots each still need publication acceptance; earlier evening catch-up delay22min. No task change or new natural-trigger observation. Supplied rich-menu manager screenshot said「已預約」; it is not proof of current activation/delivery.
6. AAPL debt conflict remains `publication_eligible=false`; `/v213/admin/test-push` previously retained `LINE_FREE_PLAN_REVIEW_REQUIRED`, not bypassed.

Measured storage/journal regression group is now locally closed; the other3 previously tracked P2 groups (options, installer identity, automation) remain, plus explicit research/UI/delivery gaps. Whole-product P0/P1/P2 totals and release eligibility are **not recertified** by957/521 passing local tests.

## Prior operator-reported operations (not re-read here)

On2026-09-12 earlier authorized work reportedly finalized snapshot `20260912T072732Z-2347c4bc506a`, Worker `d3375fe3-55e4-436c-9d12-4cff360a6578`, with20/20 original names/returns. Another reported TOP20 run was `20260912T051141Z`; NVDA outside it returned designed `LOCAL_MODEL_NOT_CONFIGURED`. These are dated reports, not currently fresh evidence. Historical local/UTC labels remain unaltered. Do not copy obsolete「no Production publication / both tasks Disabled」claims into the current overview.

## Next action

Continue LINE-first data/admission development and source-bound validation: bilingual identity, quantity/timing ledger and reproducible scenario products, macro/options sealed products and pinned consumers. Then complete visual/device review, concrete deployment/rollback authorization and fresh Windows/CI/archive/install proof. Validate the actual installed EXE and approved model without replacement stacks; update GitHub on the reviewed branch, not by claiming a local/mock PASS is a release. Do not replay old activation or silently change installed refs.
