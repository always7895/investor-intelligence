# Investor Intelligence v2.1.3 R75 release checklist

## Release decision

- P0 integrity blockers open: **0**
- CI production mutation: **false**
- Deployment authority: user-local formal activation only
- All-LIMITED publication: requires explicit second confirmation

## Verified contracts

- PowerShell and Worker publication-mode validators are aligned.
- `LIMITED_RESEARCH_CANDIDATE` requires primary evidence plus two independent publication origins/domains.
- LIMITED rows cannot be HIGH eligible, validated theses, or retain positive sensitive factors.
- BLS is optional macro context; core required families and minimum family thresholds remain enforced.
- Activation preflight reads and hashes the sealed seven-payload bundle and rechecks it before Production mutation.
- Request bodies cannot substitute a different llama.cpp model ID.
- Gateway process launch is tested in a path containing spaces and parentheses.
- Snapshot objects are read back, idempotent replay repairs missing objects, and the pointer expires before immutable objects.
- LINE broadcast dedupe uses a Durable Object transaction.
- Refresh and formal activation have cross-process locks.
- Candidate discovery has a 30-minute seed stability window.
- Windows refresh tasks use wake, network-available, and retry controls.

## Residual non-blocking risks

- Free non-Yahoo market corroboration may remain unavailable, producing an all-LIMITED report.
- Accountless Quick Tunnel availability remains below a managed named tunnel.
- Long local-model answers still use a bounded synchronous request path.
- Interactive Windows task principal reliability after logoff requires user-machine validation.
