name: V2.1.3 Final Source-Diverse R60 (Superseded)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  superseded:
    runs-on: ubuntu-latest
    steps:
      - name: Superseded notice
        run: |
          echo "The original R60 workflow is superseded by the Hotfix4 release workflow."
          echo "No build, deployment, LINE push, or Production mutation is performed here."
