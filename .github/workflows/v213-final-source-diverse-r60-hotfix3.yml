name: V2.1.3 Source-Diverse R60 Hotfix3 (Superseded)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  superseded:
    runs-on: ubuntu-latest
    steps:
      - name: Superseded notice
        run: |
          echo "Hotfix3 is superseded by the market-quality-aware Hotfix4 release workflow."
          echo "No build, deployment, LINE push, or Production mutation is performed here."
