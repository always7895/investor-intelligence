name: V2.1.3 Source-Diverse R60 Hotfix4 (Superseded)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  superseded:
    runs-on: ubuntu-latest
    steps:
      - name: Superseded notice
        run: |
          echo "Hotfix4 is superseded by Hotfix5 Atomic release."
          echo "No build, deployment, LINE push, or Production mutation is performed here."
